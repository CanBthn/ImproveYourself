import java.util.Scanner;
public class TopicOneExtra {
    public static void main(String[] args){
        Scanner sc = new Scanner(System.in);
        /*
        int Age = sc.nextInt();    // If we want to enter a string value after the name, it skips the string value
        sc.nextLine();             // With this line called Dummy, the enter value is passed and we get the string value
        String Name = sc.nextLine(); // If it were the other way around, if it were a string and then an int
                                     // There wouldn't be any problem.
        System.out.println("AGE " + age);
        System.out.println("NAME " + name);
        */
        /*
        // Enter is not mandatory when entering an int value, we can enter it by leaving a space between them.
        int age1 = sc.nextInt(); 
        int age2 = sc.nextInt(); 
        int age3 = sc.nextInt(); 
        System.out.println("Age 1 = " + age1 +" Age 2 = " + age2 + " Age3 " + age3);
        */
        System.out.println("TopicOneExtra Is Over");
    }
}